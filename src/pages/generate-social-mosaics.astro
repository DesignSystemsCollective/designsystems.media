---
// This Astro page is used to trigger image generation during the build process.
// It will not be rendered to HTML, but its server-side logic will execute.
import { runAllMosaics } from "../utils/generateSocialImages.js";

// Call the function to generate the mosaics.
// This code runs only during the server-side build.
await runAllMosaics();

// You can optionally output a blank response or a simple message.
// The content of this page won't be deployed unless explicitly linked.
// Using <Fragment> ensures no extra HTML is generated.
---

<Fragment></Fragment>
