---
import LabelCount from "../../components/LabelCount.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";
import ResponsiveGrid from "../../layouts/ResponsiveGrid.astro";
import { getTaxonomyIndex } from "../../lib/content-domain";

const title = "Tags";
const { groupedItems: groupedTags } = await getTaxonomyIndex("tags");
---

<BaseLayout sidebar={false} metadataProps={{ title }}>
  <div class="alphabet">
    {
      Object.keys(groupedTags).map((letter) => (
        <a href={`#${letter}`}>{letter}</a>
      ))
    }
  </div>
  <ResponsiveGrid>
    {
      Object.keys(groupedTags).map((letter) => (
        <div>
          <h2 id={letter}>{letter}</h2>
            <div class="grid">
            {groupedTags[letter].map((tagItem) => (
              <LabelCount
                link={`${tagItem.slug}/`}
                type="tags"
                name={tagItem.name}
                count={tagItem.count}
              />
            ))}
          </div>
        </div>
      ))
    }
  </ResponsiveGrid>
</BaseLayout>

<style>
  .alphabet {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    width: 100%;
    padding: var(--space-16);
    gap: 0.5em;
    margin-bottom: var(--space-16);
  }
  .alphabet a {
    font-size: var(--font-size-20);
    text-decoration: none;
    background: var(--tagCount-background);
    padding: 0.1em 0.6em 0em;
    border-radius: var(--radius-16);
  }
  .alphabet a:hover {
    outline: 1px solid var(--tagCount-count);
  }
  .grid {
    display: flex;
    flex-direction: column;
    gap: 0.5em;
  }
</style>
