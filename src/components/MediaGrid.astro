---
import ResponsiveGrid from "../layouts/ResponsiveGrid.astro";
import VideoCard from "./VideoCard.astro";

const { media, layoutType = "grid", gridItemMinWidth = "600px" } = Astro.props;
---

<ResponsiveGrid gridItemMinWidth={gridItemMinWidth}} gridType="grid">
  {
    media.map((media, index) => (
      <VideoCard
        title={media.data.title}
        poster={media.data.poster}
        duration={media.data.duration}
        slug={media.slug}
        publishedAt={media.data.publishedAt}  
        layoutType={layoutType}
        loading={index < 8 ? "eager" : "lazy"}
      />
    ))
  }
</ResponsiveGrid>
